---
import Layout from '../layouts/Layout.astro';
import ProjectCard from '../components/ProjectCard.astro';

const projects = [
  {
    title: "CAF Case Management System",
    description: "End-to-end internal platform for Centro de Apoyo A.C., moving manual workflows into a React/TypeScript ecosystem. Includes secure PostgreSQL backend with RBAC, role-aware case intake, and automated reporting. Deployed with CI/CD across AWS/Vercel and Docker for self-hosting.",
    image: "/assets/project-3.png",
    technologies: ["React", "TypeScript", "PostgreSQL", "RBAC", "Docker"],
    liveUrl: "#" // Video demo link will be provided
  },
  {
    title: "DEM Bravo (Docker Exploit Mapper)",
    description: "Offline-first cybersecurity platform modeling container attack graphs with Neo4j. Built FastAPI services orchestrating Docker SDK streams and a SvelteKit dashboard for topology visualization. Implemented STIG-aligned query sanitization to prevent injection during multi-tool data ingestion.",
    image: "/assets/project-1.png",
    technologies: ["FastAPI", "Neo4j", "SvelteKit", "Docker", "Python"],
    githubUrl: "https://github.com/ChristianDees/CS4311_DEM_Bravo_Fall2025",
    liveUrl: "#" // Will be provided later
  },
  {
    title: "Bank of Miners",
    description: "Modernized a legacy banking CLI into a web application. Implemented custom Binary Search Trees for O(log n) retrieval, Write-Through persistence for reliability, and integrated a React frontend with a Spring Boot REST API.",
    image: "/assets/project-2.png",
    technologies: ["Java", "Spring Boot", "React", "Docker", "PostgreSQL"],
    githubUrl: "https://github.com/BryanPMX/bank-system",
    liveUrl: "#" // Will be provided later
  }
];
---

<Layout title="Projects - Bryan Perez">
  <div class="min-h-screen">
    <!-- Header Section -->
    <section class="py-20 px-6">
      <div class="max-w-6xl mx-auto flex flex-col gap-6 text-center">
        <div class="inline-flex items-center justify-center gap-2 self-center rounded-full border border-white/10 bg-white/5 px-4 py-2 text-xs font-semibold uppercase tracking-[0.2em] text-dark-text-secondary">
          Case studies · End-to-end delivery
        </div>
        <h1 class="text-4xl lg:text-5xl font-semibold text-gradient">Projects that balance speed, reliability, and polish.</h1>
        <p class="text-lg text-dark-text-secondary max-w-3xl self-center">
          Real shipped work across civic tech, cybersecurity, finance modernizations, and games. Each project pairs robust architecture with tactile UI.
        </p>
        <div class="flex flex-wrap justify-center gap-3">
          <span class="rounded-full border border-white/10 bg-white/5 px-4 py-2 text-sm font-semibold text-dark-text-secondary">Go · Next.js · PostgreSQL</span>
          <span class="rounded-full border border-white/10 bg-white/5 px-4 py-2 text-sm font-semibold text-dark-text-secondary">FastAPI · Neo4j · Docker</span>
          <span class="rounded-full border border-white/10 bg-white/5 px-4 py-2 text-sm font-semibold text-dark-text-secondary">WebSockets · CI/CD</span>
        </div>
      </div>
    </section>

    <!-- Projects Grid -->
    <section class="pb-20 px-6">
      <div class="max-w-6xl mx-auto grid grid-cols-1 md:grid-cols-2 gap-8">
        {projects.map((project) => (
          <ProjectCard
            title={project.title}
            description={project.description}
            image={project.image}
            technologies={project.technologies}
            githubUrl={project.githubUrl}
            liveUrl={project.liveUrl}
            type="project"
          />
        ))}
      </div>
    </section>

    <!-- Delivery Principles -->
    <section class="py-20 px-6 bg-dark-surface/70">
      <div class="max-w-6xl mx-auto grid gap-10 lg:grid-cols-[1fr_1.1fr] items-center">
        <div class="space-y-4">
          <p class="text-sm uppercase tracking-[0.2em] text-dark-text-secondary">How I deliver</p>
          <h2 class="text-3xl lg:text-4xl font-semibold">Intentional processes that keep momentum.</h2>
          <p class="text-dark-text-secondary">
            From kickoff to launch, I keep teams aligned with concise docs, observable systems, and frequent demos. Reliability first, then refinement.
          </p>
          <div class="flex flex-wrap gap-3">
            <span class="rounded-full border border-white/10 px-3 py-1 text-xs text-dark-text-secondary">RFCs & architecture notes</span>
            <span class="rounded-full border border-white/10 px-3 py-1 text-xs text-dark-text-secondary">Automated QA + CI/CD</span>
            <span class="rounded-full border border-white/10 px-3 py-1 text-xs text-dark-text-secondary">Performance budgets</span>
          </div>
        </div>
        <div class="grid gap-4 md:grid-cols-2">
          {[
            { title: 'Discovery → Prototype', copy: 'Rapid technical spikes with measurable benchmarks before full build.' },
            { title: 'Production Guardrails', copy: 'Auth, RBAC, telemetry, and load testing baked into the foundation.' },
            { title: 'Design & Motion', copy: 'Micro-interactions and visual systems that stay consistent across pages.' },
            { title: 'Knowledge Transfer', copy: 'Lightweight docs, runnable demos, and handoff sessions to keep teams shipping.' }
          ].map((item) => (
            <div class="panel p-4 gradient-border">
              <h3 class="text-lg font-semibold mb-2">{item.title}</h3>
              <p class="text-sm text-dark-text-secondary leading-relaxed">{item.copy}</p>
            </div>
          ))}
        </div>
      </div>
    </section>

    <!-- Call to Action -->
    <section class="py-20 px-6">
      <div class="max-w-5xl mx-auto panel gradient-border p-10 text-center space-y-6">
        <p class="text-sm uppercase tracking-[0.2em] text-dark-text-secondary">Let’s collaborate</p>
        <h2 class="text-3xl lg:text-4xl font-semibold">Have an idea or platform that needs shipping?</h2>
        <p class="text-dark-text-secondary max-w-3xl mx-auto">
          I join teams as a builder, not just a consultant—shipping features, establishing foundations, and leaving teams with lasting systems.
        </p>
        <div class="flex flex-col sm:flex-row gap-4 justify-center">
          <a 
            href="/contact" 
            class="cta-solid"
          >
            Get In Touch
          </a>
          <a 
            href="/assets/Bryan Resume 2025.pdf" 
            target="_blank"
            class="cta-ghost"
          >
            View Resume
          </a>
        </div>
      </div>
    </section>
  </div>
</Layout>
