---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Bryan Perez - Software Engineer">
  <section class="px-6 pb-24 reveal-on-scroll">
    <div class="mx-auto max-w-6xl grid gap-12 lg:grid-cols-[1.05fr_0.95fr] items-center">
      <div class="space-y-8 animate-fade-in">
        <div class="inline-flex items-center gap-2 rounded-full border border-white/10 bg-white/5 px-4 py-2 text-xs font-semibold uppercase tracking-[0.2em] text-dark-text-secondary">
          <span class="h-2 w-2 rounded-full bg-green-400 animate-pulse"></span>
          Open for new roles - Feb 2026
        </div>

        <div class="space-y-4">
          <p class="text-sm font-semibold uppercase tracking-[0.22em] text-dark-text-secondary">Software Engineer | Secure Systems | AI Workflows</p>
          <h1 class="text-5xl lg:text-6xl font-semibold leading-tight text-gradient">
            Secure software. Sharp UX. Fast delivery.
          </h1>
          <p class="text-lg text-dark-text-secondary max-w-2xl">
            I turn complex business problems into reliable products, from planning to deployment.
          </p>
        </div>

        <div class="flex flex-wrap gap-3">
          <a href="/contact" class="cta-solid">Hire me</a>
          <a href="/projects" class="cta-ghost">View projects</a>
          <a href="/assets/Bryan Resume 2025.pdf" target="_blank" class="cta-ghost border-dashed">Resume</a>
        </div>

        <div class="flex flex-wrap gap-2">
          <span class="badge-glass">El Paso, TX</span>
          <span class="badge-glass">Full-time + freelance</span>
          <span class="badge-glass">US Mountain Time</span>
        </div>

        <div class="grid gap-4 sm:grid-cols-3">
          {[
            { title: 'CAF Platform', desc: 'Moved manual operations into a React + TypeScript system.' },
            { title: 'AI Code Quality', desc: 'Reviewed and improved Java/Python outputs for Outlier.' },
            { title: 'Security Focus', desc: 'Built RBAC backends and STIG-style hardening workflows.' }
          ].map((item) => (
            <div class="panel gradient-border interactive-card p-4">
              <p class="text-xs uppercase tracking-[0.2em] text-dark-text-secondary mb-1">{item.title}</p>
              <p class="text-sm text-dark-text-secondary">{item.desc}</p>
            </div>
          ))}
        </div>
      </div>

      <div class="relative reveal-on-scroll">
        <div class="panel gradient-border interactive-card p-6 lg:p-8">
          <div class="flex items-center justify-between gap-4">
            <div class="flex items-center gap-3">
              <div class="relative h-16 w-16 overflow-hidden rounded-2xl border border-white/10">
                <img src="/assets/profile-pic.png" alt="Bryan Perez" class="h-full w-full object-cover" />
              </div>
              <div>
                <p class="text-sm text-dark-text-secondary">Software Engineer</p>
                <p class="text-lg font-semibold">Bryan Perez</p>
              </div>
            </div>
            <a href="mailto:perez.bryan24@outlook.com" class="cta-solid hidden sm:inline-flex">Email</a>
          </div>

          <div class="mt-6 grid gap-3 rounded-2xl bg-dark-bg/60 p-4 sm:grid-cols-2">
            <div class="rounded-xl border border-white/5 bg-white/5 p-4">
              <p class="text-xs uppercase tracking-[0.2em] text-dark-text-secondary">Now building</p>
              <p class="text-sm font-semibold">Secure web apps, data systems, ML tooling</p>
            </div>
            <div class="rounded-xl border border-white/5 bg-white/5 p-4">
              <p class="text-xs uppercase tracking-[0.2em] text-dark-text-secondary">Core stack</p>
              <p class="text-sm font-semibold">Go, TypeScript, React, FastAPI, PostgreSQL, Neo4j</p>
            </div>
          </div>

          <div class="mt-6 grid gap-3 sm:grid-cols-3">
            {[
              { label: 'Education', value: 'UTEP CS + Math' },
              { label: 'Experience', value: '2023-present' },
              { label: 'Mode', value: 'Remote / On-site' }
            ].map((item) => (
              <div class="rounded-xl border border-white/5 bg-white/5 p-3">
                <p class="text-[11px] uppercase tracking-[0.2em] text-dark-text-secondary">{item.label}</p>
                <p class="text-sm font-semibold mt-1">{item.value}</p>
              </div>
            ))}
          </div>
        </div>

        <div class="pointer-events-none absolute -bottom-12 -left-6 hidden h-32 w-32 rounded-full bg-accent/20 blur-3xl sm:block"></div>
      </div>
    </div>
  </section>

  <section class="py-20 px-6 bg-dark-surface/60 reveal-on-scroll">
    <div class="max-w-6xl mx-auto space-y-12">
      <div class="flex flex-col gap-4 text-center">
        <p class="badge-glass mx-auto">What I do</p>
        <h2 class="text-3xl lg:text-4xl font-semibold text-gradient-accent">I ship products that stay fast, secure, and usable.</h2>
        <p class="text-dark-text-secondary text-lg max-w-3xl mx-auto">
          I handle architecture, implementation, and polish with measurable outcomes.
        </p>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
        <div class="panel gradient-border interactive-card p-6 space-y-4">
          <p class="text-sm text-dark-text-secondary uppercase tracking-[0.16em]">Product Engineering</p>
          <p class="text-lg font-semibold">Clear interfaces that convert.</p>
          <p class="text-sm text-dark-text-secondary leading-relaxed">
            React, Next.js, Astro, and design systems that scale with teams.
          </p>
        </div>

        <div class="panel gradient-border interactive-card p-6 space-y-4">
          <p class="text-sm text-dark-text-secondary uppercase tracking-[0.16em]">Systems and Data</p>
          <p class="text-lg font-semibold">Backends you can trust.</p>
          <p class="text-sm text-dark-text-secondary leading-relaxed">
            RBAC, API hardening, observability, and CI/CD delivery pipelines.
          </p>
        </div>

        <div class="panel gradient-border interactive-card p-6 space-y-4">
          <p class="text-sm text-dark-text-secondary uppercase tracking-[0.16em]">Research and Games</p>
          <p class="text-lg font-semibold">Experiments with real signal.</p>
          <p class="text-sm text-dark-text-secondary leading-relaxed">
            RL studies, SVM experimentation, and responsive gameplay loops.
          </p>
        </div>
      </div>
    </div>
  </section>

  <section class="py-20 px-6 reveal-on-scroll">
    <div class="max-w-6xl mx-auto space-y-10">
      <div class="flex flex-col gap-3 text-center">
        <p class="badge-glass mx-auto">Explore</p>
        <h2 class="text-3xl lg:text-4xl font-semibold">Pick a track.</h2>
      </div>
      <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
        {[
          { href: '/projects', title: 'Projects', copy: 'Shipped systems and case studies.', accent: 'from-accent to-accent-purple' },
          { href: '/games', title: 'Games', copy: 'Playable browser experiences.', accent: 'from-accent-purple to-accent' },
          { href: '/about', title: 'About', copy: 'Background, stack, and work style.', accent: 'from-accent to-white' }
        ].map((card) => (
          <a
            href={card.href}
            class="group interactive-card relative overflow-hidden rounded-2xl border border-white/5 bg-dark-surface/70 p-7 shadow-[0_18px_60px_-32px_rgba(0,0,0,0.6)] transition-all duration-300 hover:-translate-y-2 hover:border-accent"
          >
            <div class={`absolute inset-0 opacity-0 group-hover:opacity-100 transition-opacity duration-300 bg-gradient-to-br ${card.accent} blur-3xl`}></div>
            <div class="relative space-y-4">
              <div class="inline-flex items-center gap-2 rounded-full bg-white/5 px-3 py-1 text-xs font-semibold text-dark-text-secondary">
                {card.title === 'Projects' && 'Case studies'}
                {card.title === 'Games' && 'Play now'}
                {card.title === 'About' && 'Profile'}
              </div>
              <div class="flex items-center justify-between">
                <h3 class="text-xl font-semibold">{card.title}</h3>
                <div class="grid h-10 w-10 place-items-center rounded-full bg-white/5 text-dark-text transition-all group-hover:bg-dark-bg group-hover:text-white group-hover:-translate-y-1">
                  <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
                  </svg>
                </div>
              </div>
              <p class="text-dark-text-secondary text-sm leading-relaxed">{card.copy}</p>
            </div>
          </a>
        ))}
      </div>
    </div>
  </section>
</Layout>
